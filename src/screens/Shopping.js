import { View, Text, StatusBar } from 'react-native';

export default function Shopping() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Shopping.js</Text>
            <StatusBar style="auto" />
        </View>
    );
}